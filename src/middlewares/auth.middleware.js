//import {verifyToken} from "jwt.herlper"

//export const authmiddleware = (req,res,next) => {
// const Token = req.cookies.token;
//
//if (!token){
//return res.status(401).json({msg:"Token no proporcionado"})
//}
//const decoded = verifyToken(token);
//
//req.user = decoded;

//next();
//}
